import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Продажи' };

export default function SalesPage() {
  return (
    <div className="flex h-full items-center justify-center text-gray-400">
      Окно Продажи — в разработке
    </div>
  );
}
